<h1>Signup:</h1>

<%= form_for @user, url: {action: "create"} do |f| %>
  <%= render 'shared/error_messages' %>
  
  <%= f.label :username %>
  <%= f.text_field :username %>

  <%= f.label :email %>
  <%= f.email_field :email %>

  <%= f.submit "Sign Up" %>
<% end %>
